class GeneralConstants {

    static APP_ROOT_PATH: string = "${path.editor}/";
    static DEFAULT_KIT: string = "2015";
    static DASHBOARD_REST_SERVLET: string = "http://localhost:${port.dashboard}${path.dashboard}/RobotRest";
    static EDITOR_REST_SERVLET: string = "http://localhost:${port.editor}${path.editor}/editorService";
    
}